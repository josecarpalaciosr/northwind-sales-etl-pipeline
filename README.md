# Northwind Sales ETL Pipeline

A local batch ETL pipeline that extracts sales data from the Northwind SQLite database, transforms and validates it using Python, SQL, and Pandas, and loads the results into an analytical SQLite database.

## Project Status

🚧 In development